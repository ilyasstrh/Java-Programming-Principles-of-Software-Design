import java.util.*;
/**
 * Write a description of Random here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Random {
    
    
    public static void main(String[] args) {
        ArrayList<String> follows = new ArrayList<String>();
    String array = "an apple a day";
    String key = "a";
    //int pos = array.indexOf(key);
    int index = 0;
    
    for(int pos=array.indexOf(key); pos < array.length() -1; pos++){
        if(pos > -1){
            String current = array.indexOf(pos+1);
            //follows.add();
        }
    }
    }
    
    
}
